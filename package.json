{
  "name": "my-movie-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.16.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:clean": "cross-env REACT_APP_CLEAR_STORAGE=true vite",
    "clear-storage": "cross-env REACT_APP_CLEAR_STORAGE=true node -e \"console.log('🧹 localStorage will be cleared when app starts')\""
  }
}
